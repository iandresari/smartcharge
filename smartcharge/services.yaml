refresh_data:
  name: Refresh SmartCharge Data
  description: Manually refresh all charging station data from EnBW API
  target:
    integration: smartcharge

add_charge_point:
  name: Add Charge Point
  description: Add a new charge point to monitoring
  target:
    integration: smartcharge
  fields:
    entry_id:
      name: Entry ID
      description: Configuration entry ID
      required: true
      selector:
        text:

remove_charge_point:
  name: Remove Charge Point
  description: Remove a charge point from monitoring
  target:
    integration: smartcharge
  fields:
    entry_id:
      name: Entry ID
      description: Configuration entry ID
      required: true
      selector:
        text:
